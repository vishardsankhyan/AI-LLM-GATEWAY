services:
  litellm:
    image: ghcr.io/berriai/litellm:1.100.0
    ports:
      - "4000:4000"
    command:
      - "--config"
      - "/app/config.yaml"
      - "--port"
      - "4000"
    volumes:
      - ${PWD}/docker/config.yaml:/app/config.yaml:ro
